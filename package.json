{
  "packageManager": "pnpm@7.12.2",
  "name": "@web3-social/hpke-framework",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "author": {
    "name": "web3-social",
    "url": "https://github.com/web3-social"
  },
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "release": "pnpm run build && pnpm run -r build && npx changeset publish",
    "build": "tsc -b ./tsconfig.cjs.json",
    "build:watch": "tsc -b ./tsconfig.cjs.json --watch"
  },
  "devDependencies": {
    "@vitest/ui": "^0.25.4",
    "prettier": "^2.8.0",
    "rollup": "^3.4.0",
    "typescript": "4.9.3",
    "vitest": "^0.25.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": []
  }
}
